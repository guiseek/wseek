export interface MenuOptions {
  name: string;
  appRoot: string;
  path: string;
  sourceDir: string;

  project: string;
  projectName?: string;

  module: string;
  export: boolean;

  model: string;
}
