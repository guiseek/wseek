import { ArticleDataSource } from './article-data-source';

describe('ArticleDataSource', () => {
  it('should create an instance', () => {
    expect(new ArticleDataSource()).toBeTruthy();
  });
});
